# 更新记录

## 2026-09-10

- 新增 NeoTaCZ 视频设置，可直接选择流畅、普通、高、极高、超高和自定义。默认保持 2K；自定义可分别调整贴图清晰度、模型等级和细节。
- 调整画质时显示 Minecraft 原生加载进度，完成后回到设置页。没有改动不会重复加载，取消不会保存。
- 支持枪包提供的 `MSFT_lod` 分级模型；没有分级的适用模型可自动减少细节。降低清晰度后缓存处理结果，并释放旧尺寸纹理。
- mesh 枪械使用独立枪包，不再替换默认枪。第一人称、第三人称、角色预览、地面和展示物品统一使用声明的 mesh 外形。
- 物品栏、快捷栏和 HUD 可使用模型生成的二维图标，修复旧图标及裁切问题。
- 新增 mesh 配件加载，握把、消音器等可沿用现有 Z 键改装流程安装和卸下。
- 补充 KTX2/BasisU、Draco、meshopt 压缩及部分标准量化模型支持，完善贴图采样和多级纹理。
- 修复部分 8K KTX2 加载失败、画质切换后手部姿态未恢复等问题，并补充独立枪包模板和制作文档。

推送前重新构建并通过 373 项自动测试；此前已完成限定场景的 macOS 实机检查。8K 仍需要较多内存，多把高模同时显示也仍有性能压力；尚不支持所有 glTF 扩展和玻璃透射材质。详细范围见 [实测报告](docs/public-mesh-acceptance.md) 和 [渲染支持表](docs/gltf-rendering.md)。
